# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust  # 本地信任（可选）
host    all             all             127.0.0.1/32            scram-sha-256  # 本地 IP
host    all             all             ::1/128                 scram-sha-256  # IPv6
host    all             all             all                     scram-sha-256  # 所有主机（生产时改成具体 IP）
